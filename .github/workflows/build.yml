on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to v0.1: this config uses urob's Zephyr-3.2-era ZMK fork, which
    # newer versions of the shared workflow (@main) no longer support.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.1
